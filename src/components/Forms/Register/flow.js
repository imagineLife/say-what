export type propsType = {
  toggleForm: () => mixed,
  myRunLoginKey: () => mixed,
}

export type TempStateType = {
  firstName: string,
  lastName: string,
  username: string,
  password: string,
  email: string
}