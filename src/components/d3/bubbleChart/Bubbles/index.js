import React from 'react'
import './Bubbles.css';

export default ({margins, svgDimensions }) => {
  // const { height, width } = svgDimensions

  return (
    <g className='bubbleWrapper'>
    </g>
  )
}
